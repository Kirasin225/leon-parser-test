# Leonbets Async Parser

Тестове завдання на розробку асинхронного парсера прематч даних спортивних подій.

## 🛠 Технологічний стек
* **Java 21** (Core) — без використання Spring Framework для мінімізації оверхеду.
* **java.net.http.HttpClient** — для виконання HTTP-запитів (HTTP/2).
* **Jackson Databind** — для мапінгу JSON відповідей у Java-об'єкти.
* **SLF4J / Logback** — для логування процесу роботи.
* **ExecutorService** — для керування багатопоточністю.
* **Lombok**

## 🚀 Особливості реалізації
Проект виконано з суворим дотриманням вимог технічного завдання:

1.  **Асинхронність та обмеження ресурсів:**
    * Використано кастомний пул потоків (`Executors.newFixedThreadPool(3)`).
    * Парсинг ліг та матчів відбувається паралельно, але **не більше ніж у 3 потоки** одночасно.
    * Використано `CompletableFuture` для побудови ланцюжка асинхронних завдань.

2.  **Робота з даними:**
    * Реалізовано пошук **Top Leagues** через фільтрацію регіонів (пошук прапорця `top: true`).
    * Завантаження деталей ("All markets") відбувається окремим запитом для кожного матчу.
    * Час початку матчу автоматично конвертується у формат **UTC**, як вимагалось у прикладі.

3.  **Anti-Bot заходи:**
    * Реалізовано механізм ротації **User-Agent** (випадковий вибір зі списку актуальних десктопних агентів).
    * Додано **Referer** хедери для імітації переходів у браузері.

## ⚙️ Як запустити

### Вимоги
* JDK 21+
* Maven

### Збірка та запуск
1. Зберіть проект за допомогою Maven:
   ```bash
   mvn clean package
